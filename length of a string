  #include <stdio.h>

int main() {
    char str[] = "g4343"; 
    
    printf("The given string is %s\n", str);
    
    
    int length;
    
    length = sizeof str; 
  
    printf("The length of the string is %d", length-1); 
 
    return 0;
}
